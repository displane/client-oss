style.
  html,body,webview {
    margin: 0;
    padding: 0;
    width:100%;
    height: 100%;
  }

  .half-hor {
      height: 100%;
      width: 50%;
      display: inline-flex;

  }

  .half-vert {
      height: 50%;
      display: block;
  }

.half-vert
    .half-hor
        webview#webview1(src=main[0].url, guestinstance="0")
    .half-hor
        webview#webview2(src=main[1].url, guestinstance="0")
.half-vert
    .half-hor
        webview#webview3(src=main[2].url, guestinstance="0")
    .half-hor
        webview#webview4(src=main[3].url, guestinstance="0")